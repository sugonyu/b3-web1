# Development Journal — 2026-08-03 (Monday)

Milestone: MVP v0.2 — Backend Foundation and First Vertical Slice
Daily focus: Create a reproducible and safe starting state for the browser demo.

## Goal

The first Monday checkpoint was to prepare Tony, Mina, Alex, and one available
Almond listing without creating the BorrowRequest that must be demonstrated in
the browser.

## What I Faced and Attempted

The seed needed to be easy to rerun without deleting existing data or exposing a
demo password in source control. I separated it from the read-only database
inspector and implemented it as an explicit Flask CLI command under the developer
tools package.

The command reads `BOOKLOOP_DEMO_PASSWORD` from the local environment, stores only
its hash, creates missing demo records, and leaves existing matching records in
place. It intentionally does not create a BorrowRequest.

## What I Achieved

- Added the `seed-demo` Flask CLI command.
- Created Tony as borrower, Mina as Almond owner, and Alex as unrelated test user.
- Created Mina's available Almond listing.
- Kept BorrowRequest at zero so the live form remains the proof of creation.
- Documented the command and local password boundary.
- Verified that a second run creates no duplicate users or listing.
- Implemented the seeded browser login and POST logout routes with Flask-Login.
- Added the login form and authenticated state to the Python/Jinja product home.
- Verified the login session across a second HTTP request without exposing private fields.
- Added registration with required-field and duplicate-account validation.
- Stored only the password hash and automatically started the new user's session.
- Tony confirmed registration, login, refresh persistence, and logout in the browser.

## Verification

```text
Focused demo-seed tests: 3 passed
Full backend suite after authentication: 44 passed
First run: 3 users + 1 listing + 0 BorrowRequests
Second run: 0 duplicate users + 0 duplicate listings
Seeded Tony login through running Flask server: passed
Session persisted on the next HTTP request: passed
Registration, login, refresh, and logout: Tony browser confirmed
```

The automated tests used an in-memory SQLite database. After that isolated check,
the same command was run once against the local development database. Raw SQLite
inspection and `/dev/db/` confirmed three users, one Almond listing, and zero
BorrowRequests.

## Next Action

Extract the shared BorrowRequest create/read service so the JSON API and upcoming
Python/Jinja product screen use the same validation, authorization, and database
rules. Then connect the minimal request screen.
