# August 19, 2026 (Wednesday)

## Goal

Turn the Bun/TypeScript comparison into a runnable, documented study lab while
keeping it separate from the D4 product scope.

## Work completed

- Added the React + TypeScript + Bun study lab.
- Added Bun-only frontend comparisons for vanilla JS, vanilla TS and React JSX.
- Added Node and PHP backend comparison samples.
- Documented the always-on PHP API route and the WordPress REST/localhost runbook.
- Recorded the shared-dependency behavior that lets a small JSX sample run from
  the child folder.

## Verification and decision

The archive commit sequence records the lab build and documentation progression.
The PHP sample remains an Apache-served endpoint; Bun and Node examples require
their own runtime process. This distinction is part of the lesson, not a D4
feature change.

## Publication trace

- **Chapter candidate:** Runtime, package manager, frontend and API-server boundaries.
- **Reader outcome:** A reader can run the same API request against PHP, Node or Bun and compare the startup cost.
- **Decision or failure:** Use the existing Apache/PHP server for no-extra-process API checks.
- **Reusable evidence:** Archive commits `3a1f4427`, `e43f5ac8`, `63d07e5a`, `d1bd68fd`, `6e93193a`.
- **Privacy and cleanup:** Replace local-only paths and device addresses before publication.
- **Provenance:** Original samples; official Bun/WordPress behavior is documented as external reference.

## Next

Finish the D4 presentation and then decide which lab examples belong in the public study index.
